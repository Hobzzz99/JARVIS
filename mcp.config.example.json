{
  "$comment": [
    "Register the JARVIS MCP server with an MCP-compatible client.",
    "Claude Desktop: merge into claude_desktop_config.json.",
    "Claude Code:    claude mcp add jarvis -- python -m jarvis_mcp.server",
    "Replace /absolute/path/to/JARVIS with this repository's location, and",
    "keep credentials in the project .env rather than inlining them here."
  ],
  "mcpServers": {
    "jarvis": {
      "command": "python",
      "args": ["-m", "jarvis_mcp.server"],
      "cwd": "/absolute/path/to/JARVIS",
      "env": {
        "PYTHONPATH": "/absolute/path/to/JARVIS"
      }
    }
  }
}
