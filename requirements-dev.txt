# ---------------------------------------------------------------------------
# JARVIS — development dependencies
#   pip install -r requirements.txt -r requirements-dev.txt
# ---------------------------------------------------------------------------
-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
httpx>=0.27          # FastAPI TestClient transport
ruff>=0.6            # linting + formatting
